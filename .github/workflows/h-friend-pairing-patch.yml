name: H Friend Pairing Patch

on:
  push:
    branches: [feat/h-friend-pairing]
    paths:
      - ".h-friend-pairing-trigger"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout branch
        uses: actions/checkout@v5
        with:
          ref: feat/h-friend-pairing
          fetch-depth: 0

      - name: Apply deterministic patch
        run: python3 tools/h_friend_pairing_patch.py

      - name: Commit runtime integration
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add cloud/supabase/h-whatsapp-inbox/index.ts
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git commit -m "feat(h): integrate one-time friend pairing into WhatsApp runtime"
          git push origin HEAD:feat/h-friend-pairing
