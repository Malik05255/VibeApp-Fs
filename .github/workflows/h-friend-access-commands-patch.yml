name: H Friend Access Commands Patch

on:
  push:
    branches: [feat/h-owner-friend-access-commands]
    paths:
      - ".h-friend-access-commands-trigger"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout branch
        uses: actions/checkout@v5
        with:
          ref: feat/h-owner-friend-access-commands
          fetch-depth: 0

      - name: Apply deterministic patch
        run: python3 tools/h_friend_access_commands_patch.py

      - name: Commit runtime integration
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add cloud/supabase/h-whatsapp-inbox/index.ts
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git commit -m "feat(h): manage friend access from owner WhatsApp commands"
          git push origin HEAD:feat/h-owner-friend-access-commands
