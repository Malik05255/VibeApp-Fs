name: H Pairing Secret Redaction Patch

on:
  push:
    branches: [fix/h-pairing-secret-redaction]
    paths:
      - ".h-pairing-secret-redaction-trigger"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout branch
        uses: actions/checkout@v5
        with:
          ref: fix/h-pairing-secret-redaction
          fetch-depth: 0

      - name: Apply deterministic patch
        run: python3 tools/h_pairing_secret_redaction_patch.py

      - name: Commit runtime change
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add cloud/supabase/h-whatsapp-inbox/index.ts
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git commit -m "fix(h): redact owner pairing secret before inbox persistence"
          git push origin HEAD:fix/h-pairing-secret-redaction
